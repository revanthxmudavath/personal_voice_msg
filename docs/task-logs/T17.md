# T17 — Telegram recipient consent, STOP, and admin kill switch

## Status

Implementation complete (5 of 6 plan tasks are code/test tasks, all reviewed
clean at the per-task level; this is Task 6 — verification and
documentation). Executed via `superpowers:subagent-driven-development`
(fresh implementer subagent per task, task-scoped review after each). Full
task-by-task history: `.superpowers/sdd/2026-08-19-t17-telegram-consent-stop-killswitch/progress.md`
(ledger). **Not yet merged** — the project's mandatory independent
whole-branch security review (`AGENTS.md` §Agent collaboration rules:
"Security-sensitive tasks T06, T15, T16, T17, and T18 require independent
review") is still pending, to be performed by the controlling session after
this task. That review is the last gate before a PR is opened.

## Dependencies

T16b (Telegram sender migration), complete and merged to `main` before this
task started (`docs/task-logs/T16b.md`). T16b flagged the
`recipient_key`/`chat_id` decoupling as a T17 precondition; this task
resolves it (see Task 4 below).

## Design summary

Full design: `docs/superpowers/specs/2026-08-19-t17-telegram-consent-stop-killswitch-design.md`.
Full plan: `docs/superpowers/plans/2026-08-19-t17-telegram-consent-stop-killswitch.md`.

Telegram's Bot API has no chat-history-read method for bots, so inbound
consent/STOP handling can only work via `getUpdates` polling with a
durably-stored offset cursor — a different mechanism from WAHA's approach,
which is why this needed its own design pass rather than reusing T16
machinery.

- **`SCHEMA_V8`** (`database.py`): three new tables/concerns — a
  `sending_control` singleton row (enabled/disabled flag, optional
  `DisableReason`, timestamp), a `sending_control_events` audit trail
  (append-only, one row per enable/disable transition), and a
  `telegram_inbound_offset` cursor for `getUpdates` polling. Five new
  `Database` methods: `is_sending_enabled`, `disable_sending`,
  `enable_sending`, `get_telegram_inbound_offset`,
  `set_telegram_inbound_offset`. A `DisableReason` enum
  (`STOP_COMMAND`/`SENDER_BLOCKED`/`ADMIN_KILL_SWITCH`) records *why*
  sending was disabled, for the audit trail and for future admin tooling.
  A `recipient_key_for_chat_id` helper derives the delivery idempotency key
  deterministically from the enrolled `chat_id`, closing the T16b-flagged
  gap where `recipient_key` and the real destination were never
  structurally tied.
- **`consent.py`** (new): `poll_inbound_stop` — polls Telegram's
  `getUpdates` from the stored offset, advances the offset past every
  message it sees (whether or not it matches), and calls
  `disable_sending(DisableReason.STOP_COMMAND)` only when an inbound
  message's `chat.id` matches the enrolled recipient's `chat_id` *and* the
  message text is an exact `STOP` (case handling per the design spec — not
  a fuzzy match). A pure `_process_updates` helper carries the actual
  matching logic so it's testable without network I/O. `TelegramPollError`
  wraps polling failures (bounded read, chunked response, non-2xx, timeout)
  without leaking the bot token into any error string, mirroring
  `sender.py`'s existing bounded-I/O precedent.
- **`sender.py`**: a new `SenderBlocked` exception, raised when Telegram's
  403 response body indicates the specific "bot was blocked by the user"
  condition (not any generic 403 — those still fall through to the
  existing `SenderRejected` classification). A fix round
  (`85f1c47`) widened the detection's `except` clause to also catch
  `UnicodeDecodeError` on a malformed body, so a bad byte sequence can't
  crash the classifier and produce an unhandled exception instead of a
  safe `False`.
- **`delivery.py`**: `run_daily_send` gates on `is_sending_enabled()`
  *before any production or network work* — including before an
  already-`RESERVED` delivery is allowed to progress, so the admin kill
  switch (or a STOP received mid-flight) stops a send that already claimed
  its row. A new `except SenderBlocked` handler calls
  `disable_sending(DisableReason.SENDER_BLOCKED)` on the specific
  blocked-by-user 403, durably turning off future sends without owner
  intervention. A fail-closed assertion ties the caller-supplied
  `recipient_key` to the enrolled `chat_id` via `recipient_key_for_chat_id`
  — a mismatch is a hard rejection, not a pass-through, closing the T16b
  gap.
- **AST trust-boundary test** (`test_voice_enrollment_boundaries.py`):
  extended so `discovery/`, `generation/`, and `judging/` still cannot
  reach `consent.py` (by construction — `consent.py` has zero coupling to
  those modules; the test enforces this structurally, not just today).

## Implementation (5 code/test tasks + this task, `docs/superpowers/plans/2026-08-19-t17-telegram-consent-stop-killswitch.md`)

1. `SCHEMA_V8` (sending_control/sending_control_events/telegram_inbound_offset
   tables, `DisableReason` enum, `recipient_key_for_chat_id`, 5 new
   `Database` methods) — commit `f9e4614`, reviewed clean. Two deferred
   minors recorded in the ledger (a documented pseudocode-vs-implementation
   divergence where the implementation is the correct one, and a cosmetic
   line-count mismatch in the task report).
2. `consent.py` (`poll_inbound_stop`, pure `_process_updates`,
   `TelegramPollError`) — commit `c27451e`, reviewed clean. Three deferred
   minors recorded in the ledger (no dedicated fast test for the raw I/O
   wrapper beyond the credential-gated integration test — mirrors existing
   `sender.py` precedent; bounded-read constants duplicated verbatim from
   `sender.py` rather than shared; cosmetic line-count mismatches in the
   task report).
3. `sender.py`'s `SenderBlocked` for Telegram's blocked-by-user 403 —
   commit `af9801f`; fix round (commit `85f1c47`) for an Important finding
   (`_is_blocked_by_user` could raise an uncaught `UnicodeDecodeError` on a
   malformed 403 body instead of returning `False` — the `except` clause
   missed `ValueError`/`UnicodeDecodeError`), with a new unit test added.
   Reviewed clean after the fix round.
4. `delivery.py`'s disabled-sending gate (checked before any
   production/network work), the `SenderBlocked` handler, and the
   fail-closed `recipient_key`/`chat_id` assertion — commit `4148f90`,
   reviewed clean. One item the plan-runner resolved itself during review
   (grepped `test_delivery_fault_injection.py`, confirmed only the two
   touched tests reach `AUDIO_READY` and both already use
   `recipient_key_for_chat_id` — no stale literal remained). One deferred
   minor: the gate's correctness relies on the `DELIVERY_UNKNOWN`-terminal
   branch staying side-effect-free; worth a comment if that branch ever
   grows a DB write.
5. AST trust-boundary test extended to cover `consent.py` — commit
   `3571819`, reviewed clean.
6. This task: full fresh verification suite, this log, `AGENTS.md` update.
   (Independent whole-branch security review and PR are separate,
   subsequent steps — see Status above.)

Every code/test task ran through this project's fresh-implementer-per-task,
per-task-review, no-mock TDD discipline. Full task-by-task detail (commits,
reviewer findings) is in the ledger referenced under Status.

## Verification

All commands run fresh, in order, on the final diff (`main..HEAD`, 8 commits:
`16fd005..3571819`, plus this task's own commit on top):

```text
$ uv run pytest -m fast -q
544 passed, 177 deselected in 37.97s

$ uv run pytest -m integration -q
6 passed, 58 skipped, 657 deselected in 11.19s

$ uv run pytest -m security -q
65 passed, 50 skipped, 606 deselected in 7.93s

$ uv run ruff check .
All checks passed!

$ uv run mypy src
Success: no issues found in 26 source files

$ uv run python scripts/repository_policy.py all --root .
(clean, exit 0)
```

Skips in `integration`/`security` are the same env-gated pattern as T16b
(real Telegram bot token / owner test chat / voice sample not available in
this environment) — not fabricated as passing, matching this project's
established practice for that gap.

Fast-test count rose from 518 (T16b baseline) to 544: the net of this
plan's 5 code/test tasks' new/changed tests (schema migrations, sending
control, consent processing, sender-blocked detection, delivery gating,
AST boundary) plus the pre-existing suite. `mypy` source-file count rose
from 25 to 26 (`consent.py` added).

## Independent review

**Independent whole-branch security review: pending** — to be performed by
the controlling session after this task, per this project's mandatory-review
policy for T17 (`AGENTS.md` §Agent collaboration rules: "Security-sensitive
tasks T06, T15, T16, T17, and T18 require independent review"). This log
records honest per-task review results only (see Implementation above);
it does not claim a whole-branch review outcome, since none has happened
yet.

## Files changed

New: `src/personal_voice_msg/consent.py`, `tests/fast/test_consent.py`,
`tests/fast/test_sending_control.py`,
`tests/integration/test_consent_integration.py`,
`tests/security/test_sender_blocked_unit.py`.
Modified: `src/personal_voice_msg/database.py`, `src/personal_voice_msg/delivery.py`,
`src/personal_voice_msg/sender.py`, `tests/e2e/test_delivery.py`,
`tests/e2e/test_delivery_fault_injection.py`,
`tests/fast/test_database_migrations.py`, `tests/fast/test_delivery_window.py`,
`tests/fast/test_message_history.py`,
`tests/security/test_sender_error_taxonomy.py`,
`tests/security/test_voice_enrollment_boundaries.py`.

## Next step

Independent whole-branch security review of the full T17 diff
(`main..HEAD`), performed by the controlling session per the mandatory
independent-review policy. Once that passes (with any confirmed findings
fixed and re-reviewed), open a PR and merge per this project's standard
GitHub-PR workflow. After merge, T18 (cloud and container hardening) is
next per the backlog order in `AGENTS.md`.
