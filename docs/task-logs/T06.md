# T06 - Secure result-ID-based web fetcher

## Status

Complete and verified on `task/T06-secure-web-fetcher`. Commit `2e86b91`
(`T06: enforce result-scoped public web fetching`) was pushed and merged to
`main` by PR #4.

## Dependency and environment evidence

- T02 is implemented and audited.
- The complete pre-T06 fast baseline passed `209` tests.
- GitHub CLI authentication succeeds for `revanthxmudavath`.
- Docker Desktop is reachable and reported server version `28.4.0`.
- `uv sync --locked` passes with `26` audited packages.
- The canonical current plan is `F:\personal_voice_msg\IMPLEMENTATION_PLAN.md`.

## Scope and task split

- T06 owns provider-neutral trusted-hit registration, a run-scoped private
  result-ID-to-URL map, and the secure `fetch_public_page(result_id)` transport.
- T07 owns the real SearXNG-backed `search_web(query)` request/parsing and
  Trafilatura extraction. It must register only results returned in that run
  through the T06 boundary.
- T09 owns any runtime discovery-agent benchmark. T06 gives deterministic or
  agentic callers the same narrow, enforceable web boundary.
- No agent-facing API accepts a URL. Full URLs remain private deterministic
  state; callers receive opaque random result IDs.
- Web metadata and fetched content remain untrusted. T06 does not extract,
  persist, approve, generate, synthesize, queue, or send content.

## Implemented boundary

- Added `aiohttp 3.14.2` as the smallest transport dependency and locked every
  transitive dependency.
- Search registration is atomic and limited to 20 results per run. Raw and
  sanitized title/snippet sizes are bounded, HTML is reduced to text, Unicode
  controls are removed, and all result IDs contain at least 192 bits of
  randomness.
- Result IDs are run-scoped and single-use. Fabricated, URL-shaped,
  cross-session, reused, closed-session, and concurrent requests fail with the
  same privacy-safe error.
- URL validation accepts only canonical public `http:80` and `https:443`
  destinations. It rejects credentials, fragments, non-default ports,
  ambiguous numeric forms, local suffixes, Unicode whitespace, controls, and
  non-public literal addresses.
- DNS resolution has a 3-second bound, rejects an answer set containing any
  non-public address, passes only validated numeric addresses to the connector,
  disables the connector DNS cache, revalidates the chosen socket address, and
  rejects a changed answer set during the fetch.
- Manual redirects enforce the same URL and DNS checks at every hop, reject
  loops and duplicates, permit at most five redirects, and reject HTTPS to HTTP
  downgrade.
- TLS uses the platform trust store while preserving the original hostname for
  HTTP Host and certificate verification even though the validated numeric
  address is dialed. Production explicitly passes that hostname as
  `server_hostname` for SNI.
- The client ignores ambient proxy variables, `.netrc`, cookies, and automatic
  decompression. It accepts only HTML, XHTML, or plain text and requires strict
  bounded headers and response framing.
- Bodies stream under a 1 MiB hard limit with a 15-second total deadline,
  3-second connect deadline, and 3-second idle-read deadline. A session permits
  only one in-flight fetch and cannot close during that operation.
- CI runs offline security tests normally and runs the real isolated network
  harness in a separate Docker job. Public live checks remain explicitly
  opt-in.

## TDD evidence

Initial red evidence:

- The first 68 focused tests failed at collection because the discovery module
  did not exist.
- Four later fast regressions failed because Unicode whitespace in URLs and raw
  metadata above the pre-sanitization limits were still accepted.
- The expanded Docker suite had two intended failures: malformed Content-Type
  parameters were accepted, and a second fetch could overlap an active fetch.
- One redirect-count failure was traced to the test fixture itself and corrected
  without changing production behavior.

Green evidence:

- `73` focused discovery fast tests passed.
- `282` complete fast tests passed.
- `14` offline security tests passed; `30` container-only tests skipped on the
  host as designed, and no public live test was selected.
- `30` real isolated Docker HTTP/TLS security tests passed.
- `5` explicit public live checks passed against Example Domain, HTTPBin, and
  BadSSL endpoints.
- Ruff, strict mypy, locked dependency sync, and repository policy passed.
- `docker compose ... config --quiet` passed for the T06 security harness.

The real tests cover private and mixed DNS answers, changed answers, controlled
TLS hostname verification and Host behavior, wrong and untrusted certificates,
private and downgrade redirects, loops, redirect limits, malformed headers and
locations, oversized declared and streamed bodies, chunked overflow, premature
EOF, slow headers and streams, conflicting framing, compression, unsupported
content, ambient proxy/netrc settings, capability misuse, and
representation/error redaction.

## Independent review

The first read-only security review found three blocking gaps: live tests were
selected by the normal security marker, controlled TLS identity had not been
proved, and several hostile framing/timeout cases lacked real endpoint tests.
It also identified raw metadata work, overlapping lifecycle operations, actual
`.netrc` coverage, and NAT64 scope as residual concerns.

The implementation and tests were tightened for every code-level finding:

- live tests moved to a separate live-only module;
- the Docker harness proves trusted Host behavior, hostname verification, and
  TLS failures; the production call explicitly supplies `server_hostname` for
  SNI, which the fixture does not inspect independently;
- framing, downgrade, header, slow-stream, and premature-EOF cases were added;
- raw metadata is rejected before parsing;
- one in-flight operation is enforced;
- the runner contains a real `.netrc` and hostile proxy environment.

The final read-only re-review found no remaining production-code defect. It
confirmed that the earlier live-test, controlled TLS, hostile framing,
metadata-bound, concurrency, `.netrc`/proxy/cookie, and CI-separation findings
are closed. The reviewer independently reran locked sync, all 282 fast tests,
the 14 offline security tests, Ruff, strict mypy, repository policy, and Compose
validation. Docker execution was unavailable inside the reviewer's sandbox, so
the lead integrator's separate 30-case green Docker run is the authoritative
network-harness evidence.

## Explicit residual limits and deferrals

- Python blocks the standardized NAT64 well-known and local-use prefixes, but a
  deployment-specific Pref64 cannot be inferred portably from an individual
  address. T18 must prohibit NAT64 on discovery networks or supply and test the
  deployment Pref64 before release, together with container egress controls
  that prevent discovery from reaching private services.
- An HTTP parser cannot treat bytes beyond a declared smaller Content-Length as
  part of that response. The client uses one request per forced-close connection
  so those bytes cannot be reused as another response; oversized declarations,
  premature EOF, transfer/content-length ambiguity, and streamed overflow are
  tested directly.
- DNS answer changes are tested directly against the same resolver instance and
  the production redirect path reuses that resolver. T18 still owns network
  policy and container egress enforcement.
- T07 must implement real SearXNG-backed `search_web(query)` and supply only
  results returned in that run through the trusted registration adapter; it
  must not add an arbitrary-URL path.
- Public live sites are useful compatibility checks but can change externally;
  only the deterministic Docker harness is a required CI gate.

## Cleanup

Only containers and networks created under the
`personal-voice-msg-t06` Docker Compose project are removed after verification.
The reproducible test images may remain in Docker's local build cache.

## 2026-07-28 T07 prerequisite addendum

The T08 prerequisite review found that Wikimedia's automation policy requires
a descriptive, contact-bearing user agent. The production fetcher previously
sent only `personal-voice-msg/0.1`.

The smallest correction was applied without changing the T06 trust boundary:

- public fetches now send
  `personal-voice-message-bot/0.1 (https://github.com/revanthxmudavath)`;
- the public fixture returns the received user-agent beside its existing Host,
  Authorization, and Cookie diagnostics;
- the real-network test asserts the exact public project identity and still
  proves that ambient proxy, `.netrc`, cookies, and credentials are not sent.

Red and green evidence:

- the new real-network user-agent regression failed once with the old
  `personal-voice-msg/0.1` value;
- the focused rebuilt user-agent case then passed;
- the complete isolated HTTP/TLS suite initially exposed an unrelated expired
  fixture certificate (`notAfter` 2026-07-23);
- only the ephemeral Docker test certificates changed from 2-day to 3650-day
  validity so Docker build-cache reuse cannot make the harness expire;
- the rebuilt complete real-network security suite passed `30` tests.

An independent read-only security review reported no findings and recommended
approval. It confirmed that the user agent contains only public project
identity, the TLS hostname/untrusted-CA coverage remains meaningful, the
long-lived certificates have authority only inside the isolated fixture image,
and no mocks or monkeypatching were introduced.
