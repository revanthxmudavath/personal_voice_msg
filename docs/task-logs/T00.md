# T00 — Canonical repository and cloud toolchain

## Verified state on 2026-07-21

- `F:\personal_voice_msg` is the writable canonical repository.
- `git status` succeeds and the repository has a credential-free HTTPS
  `origin` remote.
- T01 through T05 exist as committed and pushed implementation history.
- Python 3.12.4, `uv`, Git, and Node 22 are installed.
- `gh auth status` succeeds for `revanthxmudavath` using HTTPS Git operations.
- Docker Desktop is running and `docker info` reports server version 28.4.0.

## Deferred prerequisite

- FFmpeg and ffprobe are not installed. They are not required for T06, but are
  a host prerequisite before T14.

The owner repaired GitHub CLI authentication and started Docker Desktop. Both
gates were independently verified outside the restricted workspace sandbox on
2026-07-21. Development agents must still revalidate them before a dependent
task and must never manufacture credentials or bypass the sandbox.

## Audit follow-up

The previous `AGENTS.md` status still described an empty T00 repository after
T05 had been implemented. Its operational status and immediate-next-step text
were updated locally to reflect the current pre-T06 hardening state. The file
remains intentionally ignored by Git at the owner's direction.
